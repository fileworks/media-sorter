/**
 * Shared UI constants.
 */

/**
 * Canonical placeholder rendered in place of a missing/empty value.
 *
 * U+2014 EM DASH. Standardised here (see Bugs L4) so every formatter and
 * component shows the same glyph instead of a mix of "—", "N/A", and "".
 */
export const NULL_PLACEHOLDER = "—";
