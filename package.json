{
  "name": "media-sorter-release",
  "version": "0.0.0-semantically-released",
  "private": true,
  "description": "Release tooling for MediaSorter. semantic-release derives the version from Conventional Commits; the app's actual version lives under frontend/ and is synced by scripts/sync-version.mjs. This package is never published to npm.",
  "scripts": {
    "release": "semantic-release",
    "release:dry": "semantic-release --dry-run"
  },
  "devDependencies": {
    "@semantic-release/changelog": "^6.0.3",
    "@semantic-release/exec": "^6.0.3",
    "@semantic-release/git": "^10.0.1",
    "semantic-release": "^24.2.0"
  }
}
